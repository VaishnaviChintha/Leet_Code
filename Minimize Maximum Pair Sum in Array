class Solution {
    public int minPairSum(int[] nums) {
        Arrays.sort(nums);
        int i=0,j=nums.length-1;
        int min = 0;
        while(i<j){
           min=Math.max(min,nums[i]+nums[j]);
           i++;
           j--;
        }
        return min;
    }
}
